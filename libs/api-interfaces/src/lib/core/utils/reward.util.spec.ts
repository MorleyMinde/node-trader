import {convertMarketStateToArray} from './convertor.util'

describe('Reward', () => {
    it('Reward function testing', () => {
      
        //biding price
        //expect(array[1]).toEqual(1.16188);
      });
});
